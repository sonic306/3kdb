#function {thousandsFormatNumber} {
  #if {%1 < 0} {
    #math result {%1 * -1};
    #format result {%M} {$result};
    #replace result {G} {B};
    #var result {-$result};
  } {
    #format result {%M} {%1};
    #replace result {G} {B};
  };
};
